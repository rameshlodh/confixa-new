#To expose deployment port(9001)
kubectl expose deployment nodejs-app --type=NodePort

#To port forward
kubectl port-forward svc/nodejs-service 8080:8080 --address 0.0.0.0


#Added minikube ip address in /etc/hosts in ingress -host: "domain.com/app"
192.168.49.2    domain.com

#To check ingress
curl -L domain.com/app
